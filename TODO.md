# TODO

- [ ] Bundle a managed Python + Demucs runtime with the packaged app, or provide an in-app first-run installer. End users should not need to install Python or Demucs manually; they should only approve the one-time Demucs model download when it is not already cached.
